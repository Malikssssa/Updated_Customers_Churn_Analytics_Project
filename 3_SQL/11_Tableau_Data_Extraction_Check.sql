-- TABLEAU DATA EXTRACTION CHECK

-- KPI dataset (executive dashboard)
SELECT * 
FROM tableau_kpi_dataset;

-- customer growth & churn trends
SELECT *
FROM tableau_customer_trends;

-- revenue impact dataset
SELECT *
FROM tableau_revenue_dataset;

-- churn driver segments
SELECT *
FROM tableau_churn_segments;

-- high risk customer list
SELECT *
FROM tableau_risk_customers;

-- cohort retention dataset (for heatmap)
SELECT *
FROM tableau_cohort_dataset;